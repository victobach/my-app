import * as React from "react";
import { Button, Text, View } from "react-native";

const FlatListComponent = (props) => { 
    return (
        <Text>Dette er vores Flatlist</Text>
    )
}

const styles = StyleSheet.create({
    container: {
      backgroundColor: '#fff',
      flex:1
    }
  });

export default FlatListComponent; 
