import * as React from "react";
import { Button, Text, View } from "react-native";

const FetchListComponent = (props) => { 
    return (
        <Text>Dette er vores Fetchlist</Text>
    )
}

const styles = StyleSheet.create({
    container: {
      backgroundColor: '#fff',
      flex:1
    }
  });

export default FetchListComponent; 