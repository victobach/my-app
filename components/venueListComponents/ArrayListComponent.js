import * as React from "react";
import { Button, Text, View } from "react-native";

const ArrayListComponent = (props) => { 
    return (
        <Text>Dette er vores Arraylist</Text>
    )
}

const styles = StyleSheet.create({
    container: {
      backgroundColor: '#fff',
      flex:1
    }
  });

export default ArrayListComponent; 